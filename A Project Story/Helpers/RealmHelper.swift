//
//  RealmHelper.swift
//  A Project Story
//
//  Created by Trevin Wisaksana on 14/10/2017.
//  Copyright © 2017 Trevin Wisaksana. All rights reserved.
//

import RealmSwift

final class RealmHelper {
    
    /*
    static func add(_ projectDraft: Project) {
        let realm = try! Realm()
        try! realm.write() {
            realm.add(projectDraft)
        }
    }
    
    static func delete(_ projectDraft: Project) {
        let realm = try! Realm()
        try! realm.write() {
            realm.delete(projectDraft)
        }
    }
    
    static func update(_ projectDraft: Project, with project: Project) {
        let realm = try! Realm()
        try! realm.write() {
            projectDraft.title = project.title
            projectDraft.projectDescription = project.projectDescription
            projectDraft.steps = project.steps
        }
    }
    
    static func retrieveProjects() -> Results<Project> {
        let realm = try! Realm()
        return realm.objects(Project.self)
    }
    */
    
}
